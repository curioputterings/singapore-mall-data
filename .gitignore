# Python venv (lives at the repo root, shared by both projects)
venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Claude Code state
.claude/

# Per-project scrapers and HTTP caches are intentionally NOT published
shopping/scrapers/
nursing/scrapers/
shopping/raw/
nursing/raw/

# Claude Code project instructions (describe scraping internals — not for public)
CLAUDE.md
shopping/CLAUDE.md
nursing/CLAUDE.md

# Legacy duplicates at repo root, superseded by shopping/data/mbs_stores.json
scrape_mbs.py
mbs_stores.json
mbs_stores.csv

# Jekyll build artifacts (Pages renders on GitHub's side; nothing local)
_site/
.jekyll-cache/
.jekyll-metadata

# Plain-md backups generated by the analytics scripts (the published reports
# are the hand-authored interactive Plotly versions in shopping/*.md).
shopping/data/_*.md
nursing/data/_*.md

# Maintainer-only project decision log (NOT for publication).
NOTES.md
